<!DOCTYPE html>
<html>
  <head>
    <title>Under Construction</title>
    <style>
      /* Reset some default styles */
      body {
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
         background-image: url('https://wallpapers.com/images/high/4k-laptop-stormy-night-on-bed-4eijg1x3ca6fdhlq.jpg');
        font-family: Arial, sans-serif;
        background-repeat: no-repeat;
        background-position: center;
        background-size: cover;
        font-family: Arial, sans-serif;
        font-family: Arial, sans-serif;
      }

      /* Glass login form container */
      .glass-container {
        background: linear-gradient(
          135deg,
          rgba(255, 255, 255, 0.25) 25%,
          rgba(255, 255, 255, 0.1) 50%,
          rgba(255, 255, 255, 0.25) 75%
        );
        backdrop-filter: blur(10px);
        border-radius: 10px;
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
        padding: 30px;
        max-width: 400px;
        width: 90%;
        animation: fadeInDown 0.5s ease;
      }

      /* Form styles */
      form {
        display: flex;
        flex-direction: column;
        gap: 15px;
      }

      label {
        font-weight: bold;
        color: #333;
      }

      input {
        padding: 12px;
        border: 1px solid #ccc;
        border-radius: 5px;
        outline: none;
        font-size: 16px;
        font-family: inherit;
      }

      input[type="submit"] {
        cursor: pointer;
        background-color: #007bff;
        color: #fff;
        border: none;
        border-radius: 5px;
        padding: 12px;
        font-weight: bold;
        font-size: 16px;
        transition: background-color 0.2s ease;
      }

      input[type="submit"]:hover {
        background-color: #0056b3;
      }

      /* Optional animation */
      @keyframes fadeInDown {
        from {
          opacity: 0;
          transform: translateY(-20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }
    </style>
  </head>
  <body>
    <div class="glass-container">
      <h1 style="text-align: center; margin-bottom: 20px;">Under Construction</h1>
      <p style="text-align: center;"><%= message %></p>
      <form action="/unlock" method="post">
        <label for="password">Unlock Password:</label>
        <input type="password" id="password" name="unlock_password" required>
        <label for="email">Email:</label>
        <input type="email" id="email" name="email" required>
        <input type="submit" value="Unlock">
      </form>
      <form action="/omniauth" method="get">
        <input type="submit" value="Sign in with Github">
      </form>
    </div>
  </body>
</html>
